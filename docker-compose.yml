services:
  web:
    build: .
    hostname: web
    volumes:
      - ./certs:/app/certs
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
      - ./certs:/certs:ro
    depends_on:
      - web
